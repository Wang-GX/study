Spring提出了控制反转(IOC)的概念，需要一个容器(可理解为生产bean的工厂)来创建这些bean对象以及维护这些bean之间的关系(DI依赖注入)，并能够提供给我们使用(@Autowired)。
应用上下文：光有容器还不够，还需要让它知道该如何装配这些bean。通过应用上下文对象将bean对象的定义和关系(通过xml或者注解实现)加载到Spring的容器中，容器就可以为我们管理这些bean了。